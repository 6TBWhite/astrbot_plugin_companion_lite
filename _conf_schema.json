{
  "Basic_Settings": {
    "description": "基础设置",
    "hint": "控制 CompanionLite 的基础开关。",
    "type": "object",
    "items": {
      "enable_message_capture": {
        "description": "启用消息捕获",
        "hint": "捕获私聊消息用于关系状态学习",
        "type": "bool",
        "default": true
      },
      "enable_llm_hook": {
        "description": "启用LLM上下文注入",
        "hint": "在LLM请求前注入关系状态上下文",
        "type": "bool",
        "default": true
      },
      "enable_silence": {
        "description": "启用沉默机制",
        "hint": "bot在状态不佳时可以选择简短回复或保持沉默",
        "type": "bool",
        "default": true
      },
      "enable_deep_reflection": {
        "description": "启用深度反思",
        "hint": "每完成配置数量的用户消息+Bot回复轮次，触发一次LLM语义校正",
        "type": "bool",
        "default": true
      },
      "main_user_ids": {
        "description": "主用户ID列表",
        "hint": "逗号分隔的用户ID。留空时插件不学习、不注入。",
        "type": "string",
        "default": ""
      },
      "log_level": {
        "description": "日志级别",
        "hint": "预留配置项",
        "type": "string",
        "default": "info"
      },
      "min_message_length": {
        "description": "最短学习消息长度",
        "hint": "低于此长度的消息不参与学习",
        "type": "int",
        "default": 2
      },
      "max_message_length": {
        "description": "最长学习消息长度",
        "hint": "高于此长度的消息不参与学习",
        "type": "int",
        "default": 500
      },
      "max_buffer_messages": {
        "description": "消息缓冲上限",
        "hint": "每个用户最多保留多少条待反思消息，防止缓冲无限增长",
        "type": "int",
        "default": 120
      },
      "recent_rate_window_seconds": {
        "description": "活跃聊天统计窗口(秒)",
        "hint": "用于判断连续快速消息的时间窗口",
        "type": "int",
        "default": 60
      },
      "bond_familiarity_floor": {
        "description": "陪伴模式熟悉度起步线",
        "hint": "/bond 时把熟悉度抬到此值（若更低）。默认55=熟人起步，不锁死后续仍可降",
        "type": "float",
        "default": 55.0
      },
      "bond_closeness_floor": {
        "description": "陪伴模式亲近度起步线",
        "hint": "/bond 时把亲近度抬到此值（若更低）。默认50=一般亲近",
        "type": "float",
        "default": 50.0
      },
      "bond_boundary_ceiling": {
        "description": "陪伴模式边界压力上限",
        "hint": "/bond 时把边界压力压到此值（若更高）。默认15=放松",
        "type": "float",
        "default": 15.0
      }
    }
  },
  "Reflection_Settings": {
    "description": "深度反思设置",
    "hint": "LLM 深度关系分析的触发条件。",
    "type": "object",
    "items": {
      "reflection_message_interval": {
        "description": "反思触发问答轮数",
        "hint": "每完成多少个用户消息+Bot回复的完整来回，立即触发一次深度反思",
        "type": "int",
        "default": 10
      },
      "reflection_time_interval_minutes": {
        "description": "未满轮次静默收尾(分钟)",
        "hint": "未达到触发轮数时，从最后一条用户消息起静默多久后提前反思收尾",
        "type": "int",
        "default": 40
      }
    }
  },
  "Continuity_Settings": {
    "description": "会话弧线与连续性",
    "hint": "按互动会话记录状态轨迹、转折点和规则结果，并注入仍有效的相处连续性。",
    "type": "object",
    "items": {
      "enable_continuity_injection": {
        "description": "启用连续性注入",
        "hint": "把最近会话结果、跨会话模式和高置信互动偏好注入上下文",
        "type": "bool",
        "default": true
      },
      "continuity_lookback_sessions": {
        "description": "趋势回看会话数",
        "hint": "生成连续性提示时聚合最近多少段已结束会话（3-20）",
        "type": "int",
        "min": 3,
        "max": 20,
        "default": 7
      }
    }
  },
  "Silence_Settings": {
    "description": "沉默机制设置",
    "hint": "bot 进入沉默模式的状态阈值。",
    "type": "object",
    "items": {
      "silence_energy_threshold": {
        "description": "能量沉默阈值",
        "hint": "能量低于此值时进入低能量沉默模式",
        "type": "int",
        "min": 10,
        "max": 90,
        "default": 25
      },
      "silence_boundary_threshold": {
        "description": "边界沉默阈值",
        "hint": "边界压力高于此值时进入防御沉默模式",
        "type": "int",
        "min": 0,
        "max": 100,
        "default": 60
      }
    }
  },
  "LivingMemory_Settings": {
    "description": "LivingMemory 协同",
    "hint": "与 LivingMemory 插件的协同方式。CL 只检测不写入，信任 LM 管理事实记忆。",
    "type": "object",
    "items": {
      "delegate_memory_to_livingmemory": {
        "description": "委托记忆给LivingMemory",
        "hint": "检测到LivingMemory活跃时，信任其管理长期记忆，CL不主动写入",
        "type": "bool",
        "default": true
      },
      "livingmemory_plugin_name": {
        "description": "LivingMemory插件名称",
        "hint": "用于检测LivingMemory插件实例的名称",
        "type": "string",
        "default": "LivingMemory"
      }
    }
  },
  "LLM_Settings": {
    "description": "LLM设置",
    "hint": "深度反思的LLM调用配置。",
    "type": "object",
    "items": {
      "reflection_provider_id": {
        "description": "深度反思LLM提供商ID",
        "hint": "留空则使用默认LLM提供商",
        "type": "string",
        "_special": "select_provider",
        "default": ""
      },
      "max_context_chars": {
        "description": "最大注入上下文字符数",
        "hint": "注入LLM的关系状态上下文最大长度（含连续性块，建议 >= 900）",
        "type": "int",
        "default": 900
      }
    }
  }
}
